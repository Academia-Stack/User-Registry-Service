server:
  port: 8000

timezones:
  zones:
    - Asia/Kolkata
    - Europe/London
    - America/New_York

spring:
  application:
    name: People Service

  datasource:
    url: jdbc:mysql://localhost:3306/CourseManagment
    username: darkmortal
    password: KAMEHAMEHA
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  data:
    redis:
      host: 127.0.0.1
      port: 6379
      timeout: 60000
      # password:   # leave blank if none